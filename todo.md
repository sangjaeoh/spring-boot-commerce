# TODO — 커머스 최소 기능 완결

2026-07-15 전수 대조 결과: 도메인 계층은 `REQUIREMENTS.md`가 "포함"으로 선언한 오퍼레이션을 사실상 전부 구현·테스트했으나, HTTP API 표면은 구매자 해피패스(가입→상품 등록→담기→체크아웃→취소→조회)만 노출한다. 아래 목록은 그 공백을 닫는다. 항목별 상세 작업 요청 프롬프트는 [`todo-prompt.md`](./todo-prompt.md)에 있다.

사용법: 새 세션에서 항목 하나를 골라 `todo-prompt.md`의 해당 프롬프트를 그대로 붙여넣는다. 각 프롬프트는 완료 시 이 파일의 해당 항목을 체크([x])하는 단계까지 포함한다.

권장 실행 순서: **2 → 3 → 1 → 4 → 5~8 → 9 → 10**. 번호는 중요도 순서이고 항목들은 서로 독립이라 이 순서가 강제는 아니다. 2·3을 먼저 두는 이유: 도메인이 완성돼 있어 컨트롤러 위임만 얹는 가장 얇은 작업이면서, 기존 흐름의 정합 결함(모든 PAID 주문이 영구 PREPARING, 결제한 회원 영구 탈퇴 불가)을 먼저 닫는다. 1번은 유일하게 리포지토리 쿼리부터 새로 만들고 설계 판단(페이지네이션·노출 필터)이 필요한 항목이라 그다음이다. 5~8은 상호 순서 무관, 9·10은 언제든 틈새에 가능하다.

공통 완료 기준(모든 항목): 기존 컨트롤러·파사드·테스트 패턴 준수, `./gradlew build` 게이트(Spotless·NullAway·Error Prone·ArchUnit) 통과, 인증·권한 도입 금지(범위 밖), 요청 범위 밖 기능 추가 금지.

## P1 — 흐름이 막히는 데드엔드

- [x] **1. 카탈로그 상품 목록 API** — 목록 조회가 없어 productId를 API 밖에서 알아야만 쇼핑이 시작된다. 유일하게 도메인 계층(ProductReader)에도 없는 신규 기능. 노출 규칙(판매중∧미삭제∧ACTIVE 변형≥1)·품절·대표가 파생 + 페이지네이션.
- [x] **2. 주문 이행 전이 API** — ship/confirmDelivery/hold/release 미노출로 모든 PAID 주문이 영원히 PREPARING. "출고 후 취소 거부"가 사문화되고, 결제한 회원은 영구 탈퇴 불가(미배송 PAID 가드).
- [x] **3. 재고 운영 API** — 재입고(increase)·수동 품절/재개·단종 미노출로 소진 = 영구 품절.
- [x] **4. 추가 변형 등록 + 변형 관리 API** — 등록 파사드가 첫 변형만 시딩해 옵션 상품(변형 2개 이상) 생성 불가. 가격 변경·활성/비활성/은퇴도 미노출.

## P2 — 요구사항 명시 기능의 API 노출

- [x] **5. 상품 관리 API** — 노출·숨김(show/hide), 이름·설명 편집, 논리삭제 미노출.
- [x] **6. 회원 관리 API** — 정지·해제(suspend/reinstate), 이름 변경(rename) 미노출.
- [x] **7. 쿠폰 정책 전환 + 내 발급 쿠폰 목록 API** — 정책 발급 가능·중지(disable/enable) 미노출. 회원별 발급 쿠폰 목록이 없어 체크아웃에서 쿠폰 ID를 쓸 연결 고리가 없다(IssuedCouponReader에 목록 메서드부터 없음).
- [x] **8. 결제 거래 추적 노출** — "승인·취소(환불) 거래를 각각 식별·추적" 요구가 API로 불충족. PaymentReader.getByOrderId는 이미 있어 얇은 노출만 필요.

## P3 — 프로젝트 완성도

- [ ] **9. 로컬 실행 구성 + README** — app-api에 datasource 설정이 없고 docker-compose·README가 없어 리포만으로 기동 불가(테스트는 Testcontainers로만 돎).
- [ ] **10. 문서 현행화(멱등 필터)** — REQUIREMENTS "향후 확장"과 DOMAIN_MODEL "명시적 범위 밖"에 멱등 필터가 미래 항목으로 남아 있으나 실제로는 common-web에 구현·배선·회귀 테스트 완료.
