# TODO — 운용 가능한 최소 커머스

2026-07-15 분석 결과: 이전 단계(커머스 최소 기능 완결, 10개 항목 전부 완료 — 이 파일의 git 이전 버전 참조)로 `REQUIREMENTS.md`가 "포함"으로 선언한 기능은 도메인·API 모두 완결됐다. 남은 공백은 대부분 문서가 의도적으로 "제외"·"향후 확장"으로 선언해 둔 것들이다. "실제 운용 가능한 최소 커머스"에 도달하려면 그 선언을 갱신하면서 네 축을 닫아야 한다: 신뢰 경계(인증·인가 — 현재 memberId 자기 신고·소유권 미검사), 돈의 경로(무조건 성공하는 stub PG·유실 가능한 in-process 이벤트·배송 후 환불 불가), 운영 기반(health·메트릭·CI·앱 이미지·멱등 영속화 전무), 기능 완성도. 항목별 상세 작업 요청 프롬프트는 [`todo-prompt.md`](./todo-prompt.md)에 있다.

사용법: 새 세션에서 항목 하나를 골라 `todo-prompt.md`의 해당 프롬프트를 그대로 붙여넣는다. 각 프롬프트는 완료 시 이 파일의 해당 항목을 체크([x])하는 단계까지 포함한다.

아래 목록 순서가 곧 권장 실행 순서다. 1(CI)이 맨 앞인 이유: 가장 작은 독립 작업이면서 이후 모든 항목이 원격 게이트 위에서 돌게 된다. 2→3→4는 순차 의존(자격증명 → 주체 강제 → 역할 가드)이고 기존 엔드포인트 형상 전반을 바꾸므로 다른 API 작업보다 먼저 끝내야 재작업이 없다. 6은 5 이후 착수 여부를 재평가한다. 8~10은 상호 독립이라 이 순서가 강제는 아니다. 11~15는 틈새 어느 때든 가능하되 11은 3번 이후, 13은 4번 이후가 자연스럽다.

공통 완료 기준(모든 항목): 기존 컨트롤러·파사드·테스트 패턴 준수, `./gradlew build` 게이트(Spotless·NullAway·Error Prone·ArchUnit) 통과, 요청 범위 밖 기능 추가 금지. 구현이 `REQUIREMENTS.md`·`DOMAIN_MODEL.md`의 "제외"·"향후 확장" 선언과 충돌하는 항목은 그 선언의 갱신(무엇이 범위로 들어오고 무엇이 계속 밖인지)을 같은 작업에 포함한다 — 문서 편집은 AGENTS.md 편집 규약을 따른다.

의도적 제외: 카탈로그 검색·카테고리. 현재 탐색 수단은 노출 상품 페이지뿐이지만, 상품 수가 적은 최소 운용에서는 불요로 판단했다. 필요해지는 시점에 별도 결정한다.

## 선행 — 원격 게이트

- [ ] **1. CI 파이프라인** — `.github` 부재로 빌드 게이트가 로컬 `./gradlew build`에만 존재. GitHub Actions로 push/PR 게이트 강제. 가장 작은 독립 작업이라 맨 앞 — 이후 모든 항목이 원격 게이트 위에서 돈다.

## 신뢰 경계: 인증·인가

- [x] **2. 회원 자격증명·로그인 API** — 인증 코드가 전무하고 회원에 자격증명(패스워드)이 없다. 가입에 패스워드 추가 + 로그인 + 토큰 발급. "인증·자격증명 범위 밖" 선언을 바꾸는 결정이 선행된다.
- [x] **3. 인증 주체 강제** — memberId가 경로·쿼리·본문 세 방식으로 자기 신고되고 소유권 검사가 없어 누구든 타인의 장바구니·주문·쿠폰을 조작할 수 있다. 구매자 셀프서비스 표면에서 자기 신고를 제거하고 토큰 주체로 대체.
- [x] **4. 관리자 역할 분리** — 회원 정지·재고 조작·상품 삭제·쿠폰 정책·이행 전이 같은 관리자 오퍼레이션이 구매자와 같은 표면에 무방비 노출. 역할(BUYER/ADMIN) 가드 도입.

## 돈의 경로: 결제 현실화·내구성·환불

- [x] **5. 결제 현실화** — StubPaymentGateway가 모든 승인을 무조건 성공시켜 실 운용 불가. 실패·지연 가능한 PG + 응답 유실 대비 PENDING 리컨실 + 웹훅 확정 경로. "향후 확장" 1순위로 이미 선언된 항목.
- [x] **6. 아웃박스(내구성 이벤트)** — 이벤트가 in-process 전달뿐이라 크래시 시 유실(코드 주석도 인정). 현재 소비자는 장바구니 비우기 하나라 영향이 작으므로, 5번(결제 현실화) 이후 내구성이 필요한 이벤트 소비가 생겼는지 보고 착수 여부·범위를 재평가한다. 2026-07-15 재평가 결과 보류로 종결: 5번 이후에도 발행은 `OrderModifier.markPaid()`의 `OrderPaid` 하나, 소비는 `OrderPaidListener`(장바구니 비우기 — 비필수·멱등·실패 삼킴)뿐이라 내구성이 필요한 이벤트 소비가 없다. 5번의 내구성 요구는 이벤트가 아니라 상태 기반 리컨실(REQUESTED 스윕·웹훅이 PG 상태 조회로 확정)로 충족됐고, REQUIREMENTS.md도 아웃박스를 "계속 범위 밖"으로 재선언했다. 내구성이 필요한 이벤트 소비가 처음 생기는 시점(예: 7번 환불이 이벤트 소비를 도입하는 경우)에 착수를 재결정한다.
- [x] **7. 배송 후 환불(전체 주문 반품)** — SHIPPED 이후 취소 거부는 의도된 규칙이나 대체 경로가 없어 DELIVERED 주문은 영구 환불 불가 — 운용 CS의 데드엔드. 전체 주문 단위 반품→전액 환불만 열고 부분 반품은 계속 범위 밖.

## 운영 기반

- [x] **8. 관측성** — Actuator·메트릭·구조화 로깅·management 설정 전무. 유일한 헬스체크가 compose의 pg_isready. health(liveness/readiness) + 구조화(JSON) 로깅 최소 구성. 2026-07-15 완료: actuator health(+프로브)만 노출, 구조화 로깅은 Boot 내장 ECS 포맷(전 프로필 동일 — local 별도 포맷 분기 없음). 판단 기록 — metrics 엔드포인트는 수집기 부재로 소비자가 없어 비노출, 요청 상관관계(traceId성 MDC)·micrometer-tracing은 애플리케이션 로그 지점이 OrderPaidListener 한 곳뿐이고 로그 소비가 콘솔 단일 스트림이라 보류(요청 경로 로깅·수집기 도입 시 재평가).
- [x] **9. 앱 컨테이너화** — compose가 DB만 띄우고 앱 이미지가 없다. app-migration(원샷)·app-api 이미지 + compose 풀스택 기동으로 "클론 → 명령 하나"를 완성.
- [ ] **10. 멱등 키 저장소 영속화** — InMemoryIdempotencyStore는 재시작 시 소실·다중 인스턴스 무력. DB 기반 구현으로 교체해 중복 결제 방어를 보장으로 승격.

## 기능 완성도

- [ ] **11. 주문 목록 페이지네이션** — `GET /orders`가 전체 List 반환이라 회원당 무한 성장. 상품 목록의 기성 Page 패턴 이식. 발급쿠폰 목록은 정책당 1회 발급이라 바운드 — 제외.
- [ ] **12. 출고 운송장 기록** — 이행 상태(SHIPPED·DELIVERED)는 있으나 운송장 필드가 없어 "출고했다"만 안다. ship 시 운송장 번호 기록·조회 노출(택배 연동·추적은 계속 범위 밖).
- [ ] **13. 쿠폰 발급 한도·발급분 무효화** — 정책 발급이 무제한이고, 잘못 발급된 쿠폰을 회수할 수단이 없다(정책 DISABLE은 신규 발급만 차단). 둘 다 "향후 확장"으로 이미 선언됨.
- [ ] **14. API 문서화(OpenAPI)** — 38개 엔드포인트가 코드 Javadoc으로만 문서화. springdoc-openapi 자동 생성 + swagger-ui.
- [ ] **15. Clock 주입** — 엔티티·파사드가 `Instant.now()`를 직접 호출해 시간 규칙(쿠폰 만료·발급기간)의 시간 고정 테스트가 불가능한 구조. 시간 규칙 지점 우선의 외과적 리팩터.
