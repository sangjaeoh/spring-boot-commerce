# TODO — 운영 도구·하드닝

2026-07-16 분석 결과(API 표면·운영 기반·돈의 경로·테스트/품질 4축 점검): 이전 단계(운용 가능한 최소 커머스, 15개 항목 — 이 파일의 git 이전 버전 참조)에서 2~15는 완료됐고 1번(CI)만 미완이라 이 단계로 이월한다. 도메인 기능과 품질 게이트(컨벤션 플러그인·NullAway·Error Prone·ArchUnit·Testcontainers 영속 테스트)는 완결·실배선 상태다. 남은 공백은 기능이 아니라 운영 도구와 하드닝이다: 관리자 조회 표면 부재(관리자 쓰기 오퍼레이션 26개에 대응하는 조회가 0개 — 출고 대상 주문·정지 대상 회원·무효화 대상 발급분을 API로 찾을 수 없다), 리컨실 사각지대(체크아웃의 주문 생성~결제 요청 사이 크래시가 남기는 PENDING 주문·차감 재고는 payment 행이 없어 어떤 스윕도 발견하지 못한다), 원격 게이트·보안(로그인 브루트포스 방어)·런타임(graceful shutdown·커넥션 풀)·인덱스(리컨실 스윕 풀스캔) 하드닝, 그리고 핵심 비기능 요구(오버셀 없음)의 동시성 테스트 공백이다. 항목별 상세 작업 요청 프롬프트는 [`todo-prompt.md`](./todo-prompt.md)에 있다.

사용법: 새 세션에서 항목 하나를 골라 `todo-prompt.md`의 해당 프롬프트를 그대로 붙여넣는다. 각 프롬프트는 완료 시 이 파일의 해당 항목을 체크([x])하는 단계까지 포함한다.

아래 목록 순서가 곧 권장 실행 순서다. 1(CI)이 맨 앞인 이유는 이전 단계와 같다 — 유일한 이월 항목이면서 이후 모든 항목이 원격 게이트 위에서 돈다(단, 이 저장소는 아직 git 원격이 없어 GitHub 리포 생성·푸시가 선행된다). 2(인덱스)는 마이그레이션 두 장짜리 최소 독립 작업이라 앞에 둔다. 3(관리자 조회 표면)은 배치 결정(app-admin 신설 여부)이 걸린 가장 큰 항목이고, 4(PENDING 스윕) 결과를 관측하는 수단이기도 해 4보다 먼저 둔다. 5~7은 상호 독립이다. 8은 4 완료 후(@Scheduled가 2개가 된 시점) 착수 여부를 재평가한다. 9·10은 틈새 어느 때든 가능하다.

공통 완료 기준(모든 항목): 기존 컨트롤러·파사드·테스트 패턴 준수, `./gradlew build` 게이트(Spotless·NullAway·Error Prone·ArchUnit) 통과, 요청 범위 밖 기능 추가 금지. 구현이 `REQUIREMENTS.md`·`DOMAIN_MODEL.md`의 선언과 충돌하는 항목은 그 선언의 갱신(무엇이 범위로 들어오고 무엇이 계속 밖인지)을 같은 작업에 포함한다 — 문서 편집은 AGENTS.md 편집 규약을 따른다.

의도적 제외(이번 단계에서 다루지 않기로 판단한 것):

- 메트릭 노출·요청 상관관계(traceId성 MDC): 이전 단계 8번의 보류 판단 유지. 3·4번으로 운영 조회·스윕 로그 지점이 늘면 재평가 조건에 가까워진다.
- 아웃박스: 보류 종결 유지 — 내구성이 필요한 이벤트 소비가 여전히 없다(소비는 장바구니 비우기 하나).
- 발급쿠폰 목록 페이지네이션: 정책당 1회 발급이라 바운드 — 기존 판단 유지.
- 구매자 셀프 반품 요청 워크플로·배송비 서버 산정: REQUIREMENTS.md 제외 선언 유지.
- 커버리지 리포트(Jacoco): CI 위에 얹을 후보이나 소비자(리뷰 절차)가 없어 이번 범위 밖. CI 정착 후 별도 결정.
- Money.multiply의 long 오버플로 미검·fake PG 트리거 금액(끝자리 999/998) 오탐 가능성: 비현실적 금액·연습 한계로 언급만 남긴다.
- 카탈로그 검색·카테고리: 이전 단계 판단 유지(최소 운용에서 불요).

## 선행 — 원격 게이트

- [ ] **1. CI 파이프라인** (이월) — `.github` 부재로 빌드 게이트가 로컬 `./gradlew build`에만 존재. GitHub Actions로 push/PR 게이트 강제. 저장소에 git 원격이 없어 GitHub 리포 생성·푸시가 선행 결정이다(공개 리포면 Actions 무료, 비공개도 월 2,000분 무료 티어에 기본 지출 한도 $0라 초과 과금 없음).

## 운영 도구: 돈·주문의 관측과 복구

- [ ] **2. 조회 경로 인덱스 보강** — 리컨실 스윕(`findByStatusAndCreatedAtBefore`)이 payment 인덱스 부재로 1분마다 풀스캔이고, 주문 목록 ID 페이지 쿼리는 단일 컬럼 인덱스라 정렬이 filesort. payment 부분 인덱스 + ordering 복합 인덱스 마이그레이션 두 장.
- [ ] **3. 관리자 조회 표면** — 관리자 쓰기 26개에 대응하는 조회가 0개: 주문 상태 목록(출고 대상 발견), 회원 검색, 쿠폰 정책 목록(CouponReader 자체가 없음), 정책별 발급분 목록, 재고 현황, 숨김 포함 상품 목록. app-admin 신설 vs app-api 확장 배치 결정이 선행된다(REQUIREMENTS.md "관리자 전용 앱 분리" 제외 선언과의 정합 포함).
- [ ] **4. PENDING 주문 스윕(주문 기준 리컨실)** — 체크아웃의 주문 생성~결제 요청 사이 크래시가 남기는 PENDING 주문+차감 재고+사용 쿠폰은 payment 행이 없어 기존 결제 리컨실이 영원히 발견 못 한다(팬텀 품절, 유일한 무한 잔존 경로). 유예 지난 PENDING 주문을 주기 스윕이 보상 종결한다.

## 하드닝

- [ ] **5. 보안 하드닝** — 로그인 브루트포스 방어 전무(무제한 시도 가능), CORS·시큐리티 헤더 미정의. 로그인 레이트리밋(Redis, 초과 429) + 헤더 최소셋 + CORS 정책 결정 기록.
- [ ] **6. 런타임·컨테이너 하드닝** — graceful shutdown 미설정(기본 immediate — 재시작 시 진행 중 결제 요청 절단), HikariCP 전부 기본값, compose restart 정책 없음, 컨테이너 root 실행. 각 몇 줄짜리 설정을 하나의 작업으로 묶는다.

## 검증 강화

- [ ] **7. 재고 동시성 테스트** — "오버셀 없음"이 비기능 요구인데 재고 경합 멀티스레드 테스트가 없다(쿠폰 한도 경합만 검증됨). 동시 체크아웃 경합 테스트 + 낙관락 충돌 표면 동작(재시도 없이 409) 유지 여부 결정.

## 재평가

- [ ] **8. 스케줄러 분산 락** — @Scheduled 스윕(4번 후 2개)에 분산 락이 없어 다중 인스턴스 시 전 노드가 동시 실행. 확정 경로가 멱등이라 정합성은 무해 — 단일 인스턴스 전제 선언으로 보류 종결 vs ShedLock(Redis) 도입을 재평가한다(멱등 저장소의 Redis 채택 근거였던 다중 인스턴스 전제와의 일관성이 기준).

## 기능 완성도·정리

- [ ] **9. 쿠폰 할인 미리보기** — 할인 계산이 체크아웃 내부에만 있어 구매자가 사전에 할인액을 알 수 없다. 기존 `IssuedCouponReader.calculateDiscount`를 본인 발급분 조회로 노출.
- [ ] **10. 회원 사유 파라미터 검증 일관화** — MemberController의 suspend·withdraw만 사유를 `@RequestParam`으로 받아 다른 사유 액션(`@Valid` 본문 DTO)과 오류 응답 형상이 갈린다. 본문 DTO로 통일.
