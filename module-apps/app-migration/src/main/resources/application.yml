spring:
  application:
    name: app-migration
  # 스키마별 Flyway 실행은 SchemaFlywayFactory가 프로그램적으로 담당한다. Boot 기본 Flyway는
  # classpath:db/migration을 재귀 스캔해 7개 V1__ 스크립트를 버전 충돌로 읽으므로 반드시 끈다.
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
